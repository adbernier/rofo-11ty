<div class="content-card">
  <div class="eyebrow">Example Buildings</div>
  <h2>Example {{ spaceType.pluralLabel | lower }} in {{ city.city }}</h2>

  {% if representativeBuildings and representativeBuildings.length %}
    <div class="building-grid">
      {% for building in representativeBuildings.slice(0, 6) %}
        {% include "partials/shared/building-card.njk" %}
      {% endfor %}
    </div>
  {% else %}
    {% include "partials/space-type/no-inventory-card.njk" %}
  {% endif %}
</div>
