# Company CSV Staging

`raw/` contains approved company CSV exports that are currently used by the site.

`staging/` contains candidate company CSV exports awaiting audit.

Only move files from `staging/` to `raw/` after they have been reviewed and approved for live processing.
